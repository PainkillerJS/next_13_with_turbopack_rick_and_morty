import type { FC, PropsWithChildren } from 'react';

const LayoutCharasters: FC<PropsWithChildren<unknown>> = ({ children }) => {
  return (
    <div>
      aaaaa
      {children}
    </div>
  );
};

export default LayoutCharasters;
